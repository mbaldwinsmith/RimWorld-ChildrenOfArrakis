# Textures

ArtEngine drafts live here. Use subfolders under `Things`, `Terrain`, `Pawns`, and `UI` to mirror RimWorld texture conventions. Replace placeholders with RimWorld-style sprites during art passes.
